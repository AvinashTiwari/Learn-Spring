server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AvinashTiwari/config
          username:
          password:
          search-paths: userservice, itemservice
        encrypt:
          enabled: false